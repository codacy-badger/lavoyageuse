<% content_for(:title_tag) { t('.title') } %>
<div class="flex-parent"> <!-- for IE bug -->
  <div class="fullscreen relative text-left lv-one-page" style=" background-image: linear-gradient(0deg, rgba(10,10,10,0.7) 0%, rgba(10,10,10,0.7) 100%), url('https://res.cloudinary.com/dfcsmghw4/image/upload/v1536852011/photo-1491438590914-bc09fcaaf77a.jpg')">
    <div class="form_login center">
      <h1><%= t('.layout-title') %></h1>
      <p><%= t('.needed') %></p>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>
        <div class="login-box margin-bottom-big">
          <div class="form-inputs">
            <div class="label-separator flexbox">
              <p class="label"><%= t('.data-connexion') %></p>
              <div class="separator"></div>
            </div>
            <div class="input-parent">
              <i class="far fa-envelope"></i>
              <%= f.input :email, required: true, label: false, input_html: { autocomplete: "email" }, placeholder: t('.email') %>
            </div>

            <div class="input-parent">
              <i class="fas fa-lock"></i>
              <%= f.input :password,
                          required: true,
                          placeholder: t('.password'),
                          label: false,
                          hint: t('.password_hint'),
                          input_html: { autocomplete: "new-password" } %>
            </div>
            <div class="input-parent margin-bottom-big">
              <i class="fas fa-lock"></i>
              <%= f.input :password_confirmation,
                        required: true,
                        placeholder: t('.password2'),
                        label: false,
                        input_html: { autocomplete: "new-password" } %>
            </div>

            <div class="label-separator flexbox">
              <p class="label"><%= t('.data-personal') %></p>
              <div class="separator"></div>
            </div>
            <div class="margin-bottom-small">
              <small><%= t('.restrictions') %></small>
            </div>
            <div class="input-parent">
              <i class="far fa-user"></i>
              <%= f.input :first_name, label: false, required: true, placeholder: t('.first_name') %>
            </div>

            <div class="input-parent">
              <i class="far fa-user"></i>
              <%= f.input :last_name, label: false, required: true, placeholder: t('.last_name') %>
            </div>

            <div class="input-parent">
              <i class="fas fa-phone"></i>
              <%= f.input :phone, label: false, required: true, placeholder: t('.phone') %>
            </div>

            <div class="input-parent">
              <i class="fas fa-map-marker-alt"></i>
              <%= f.input :address, label: false, required: true, placeholder: t('.address') %>
            </div>

            <div class="input-parent">
              <i class="fas fa-birthday-cake"></i>
              <%= f.input :birth_date, label: false, required: true, placeholder: t('.birth_date')  %>
            </div>

            <p>Un tout petit mot à propos de vous :</p>
            <div class="input-parent">
              <i class="fas fa-quote-right"></i>
              <%= f.input :sentence, label: false, required: true, placeholder: t('.sentence') %>
            </div>

           <div class="input-parent">
            <p><%= t('.photo') %></p>
            <!-- <%= f.input :photo, label: false %> -->
            <div class="form-group file required user_photo">
              <input class="form-control-file file required" type="file" name="user[photo]" id="user_photo">
            </div>
            <%= f.input :photo_cache, as: :hidden %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, t('.submit') %>
          </div>
        </div>
      <% end %>
        <div class="full-width" style="text-align: center">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>

<!--
            <div class="input-parent">
              <p><#= t('.photo') %></p>
              <div class="upload-container">
                <label class="text-center fileUpload">
                   <span class=""><i class="fa fa-camera upload"></i></span>
                   <#%= f.input :photo, label: false  %>
                   <#%= f.input :photo_cache, as: :hidden %>
                </label>
              </div>
            </div> -->
