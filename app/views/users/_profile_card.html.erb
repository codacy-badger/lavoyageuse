<% if user.validated_host? || moderation %>
  <%= link_to user_path(user) do %>
    <%= render "profile_card_content", user:user %>
  <% end %>
<% else %>
  <%= render "profile_card_content", user:user %>
<% end %>
