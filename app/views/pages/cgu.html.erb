<div class="container pt-5">
  <div class="row">
    <div class="col-12 col-lg-8 offset-lg-2">
      <div class="maintitle">
        <h2><strong><%= t('.title') %></strong></h2>
      </div>
      <div class="bar-separator"></div>
      <% @cugs.each do |cgu| %>
        <p><strong><%= cgu[1][:title] %></strong></p>
        <p><%= cgu[1][:content] %></p>
      <% end %>
    </div>
  </div>
</div>
